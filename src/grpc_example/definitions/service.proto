syntax = "proto3";

message Null {}

message Honey {
  string name = 1;
  string description = 2;
  uint32 price = 3;
}

message Confirmation {
  string date = 1;
}

service TestService {
  rpc Health(Null) returns (Null);
  rpc AddHoney(Honey) returns (Confirmation);
}
